{{!-- will need to reseach/ask TA with regards to implementing handlebar or process to ensure that we are all pulling from one category at a time ie (looking to pull only from the Pants category. Additionally once pulling form that category will need to find a way to cross-reference the Post and Post ID so each post will have its respective items listed. Not sure of a way to do it using handle bars --}}



{{#category}}
<div class="container">
    <div class="row">
        <div class="col s12 cards-container">
            <h2 class='text-center'>{{name}}</h2>
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title"><em>{{this.Items.name}}</em></span>
                    <p>I am a very simple card. I am good at containing small 
                        bits of information. I am convenient because I require 
                        little markup to use effectively.
                    </p>

                    <div class="content">
                    {{#this.Items}}
                    <div class="Items">
                
                    <p>Name: {{name}}</p>
                    <p>Brand: {{brand}}</p>
                    <p><img src="{{photoUrl}}"></p>
                    <p>Price: {{price}}</p>
                    <a href="{{purchaseURL}}" target="_blank">{{name}}</a></p>
                    <p><a href="{{purchaseUrl}}"></a></p>
                </div>
                 {{/this.Items}}
                </div>
                
    {{!-- {{#each item}}
    <ul>
        <li><p>{{this.name}}</p></li>
        <li><p>{{this.brand}}</p></li>
        <li><p>{{this.name}}</p></li>
        <li><p>{{this.price}}</p></li> 
    </ul>
    {{/each}}  --}}
        </div>
    
   
            </div>
        </div>
    </div>
</div>
{{/category}}
