{{!-- will need to reseach/ask TA with regards to implementing handlebar or process to ensure that we are all pulling from one category at a time ie (looking to pull only from the Pants category. Additionally once pulling form that category will need to find a way to cross-reference the Post and Post ID so each post will have its respective items listed. Not sure of a way to do it using handle bars --}}



{{#each category}}
<div class="container">
    <div class="row">
        <div class="col s12 cards-container">
            <div class="card blue-grey darken-1">
    
    <ul>
        <li><p>{{this.name}}</p></li>
        {{!-- <li><p>{{this.Items.name}}</p></li>
        <li><img href="{{this.Items.purchaseUrl}}"/></li>
        <li><img href="{{this.Items.photoUrl}}"/></li>
        <li><p>{{this.Items.price}}</p></li> --}}
    </ul>
   
            </div>
        </div>
    </div>
</div>
 {{/each}}
